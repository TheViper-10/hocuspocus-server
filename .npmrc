# Registry configuration for DevRev packages
@devrev:registry=https://npm.pkg.github.com

# Authentication token (set GITHUB_ACCESS_TOKEN environment variable)
//npm.pkg.github.com/:_authToken=${GITHUB_ACCESS_TOKEN}
